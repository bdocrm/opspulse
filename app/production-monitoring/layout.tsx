import { AppShell } from "@/components/layout/app-shell";

export default function ProductionMonitoringLayout({ children }: { children: React.ReactNode }) {
  return <AppShell>{children}</AppShell>;
}
